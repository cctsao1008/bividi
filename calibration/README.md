# Calibration artifacts

Small calibration parameter files and manifests may live here after Issue #8 defines the workflow and schema.

A calibration artifact must identify enough context to avoid accidental reuse across incompatible devices or modes, such as source/device identity, image geometry/mode, calibration session/version, and relevant processing assumptions.

Large calibration image collections do not belong in ordinary Git history by default.

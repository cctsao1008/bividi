# Device notes

This directory contains durable, source-backed facts for concrete stereo devices.

The first device document will be created under Issue #3 for the Waveshare AR0144 Stereo USB Camera (A).

Device documents must distinguish vendor claims, generic documentation, inference, unknowns, and measured behavior. Device-specific facts must not silently become core Bividi contracts.

# Characterization

This directory contains measured behavior and repeatable characterization protocols.

Planned topics are tracked in Issues #4-#10: USB/UVC enumeration, stereo transport, synchronization, calibration/rectification quality, depth behavior, latency, throughput, and operating envelope.

Measured behavior must remain distinct from vendor claims and from synthetic-test results.
